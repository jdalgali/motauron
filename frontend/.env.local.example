# Local development — points the frontend at the local Rust API server.
# Copy this to .env.local and start the backend with: cargo run -- --serve
VITE_API_URL=http://localhost:3001

# Production (Firebase) — leave VITE_API_URL unset and fill in the key below.
# VITE_FIREBASE_API_KEY=your-key-here
